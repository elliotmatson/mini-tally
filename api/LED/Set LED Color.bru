meta {
  name: Set LED Color
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/led/color?color={{color}}
  body: none
  auth: none
}

query {
  color: {{color}}
}

vars:pre-request {
  color: "red"
}
