meta {
  name: Set LED State
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/led?color={{color}}&brightness={{brightness}}
  body: none
  auth: none
}

query {
  color: {{color}}
  brightness: {{brightness}}
}

vars:pre-request {
  color: ion
  brightness: 255
}
